<Page
    x:Class="App1.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox HorizontalAlignment="Left" Margin="163,172,0,0" TextWrapping="Wrap"
                 Text="{x:Bind MyVM.MyPerson.Text,Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="280,172,0,0" TextWrapping="Wrap"
                 Text="{x:Bind MyVM.MyPerson.Text,Mode=TwoWay}" VerticalAlignment="Top"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="540,172,0,0" 
                Click="{x:Bind MyVM.Lade}"
                VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="804,172,0,0"
                   
                   TextWrapping="Wrap" Text="{x:Bind MyVM.MyPerson.Text,Mode=OneWay}" VerticalAlignment="Top"/>

    </Grid>
</Page>
